# Database (Docker)
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=

# pgAdmin (Docker)
PGADMIN_DEFAULT_EMAIL=
PGADMIN_DEFAULT_PASSWORD=

# Backend (local)
PORT=
DATABASE_URL=

# JWT Authentication
JWT_SECRET=

# Frontend (local)
VITE_API_URL=
CLIENT_URL=

# Event-driven image processing (LocalStack in development, AWS in production)
AWS_REGION=ap-south-1
AWS_ENDPOINT_URL=http://localhost:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
S3_BUCKET_NAME=snapstock-images
ANALYSIS_REQUEST_QUEUE_NAME=snapstock-analysis-requests
ANALYSIS_RESULT_QUEUE_NAME=snapstock-analysis-results

# SMTP Configuration
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
